name: dio_retry_it_tests_bundle
description: Test cases of dio_retry_it package.
publish_to: 'none'
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  dio: ^5.10.0
  dio_retry_it:
    path: ../

dev_dependencies:
  test: ^1.31.2
